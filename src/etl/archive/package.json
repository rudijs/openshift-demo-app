{
  "name": "etl",
  "version": "1.0.0",
  "description": "docker exec -it src_database_1 psql --user me -d api -c 'SELECT * FROM users;'",
  "main": "load-database.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "pg": "^8.5.1",
    "pgtools": "^0.3.0"
  },
  "devDependencies": {
    "mocha": "^8.2.1"
  }
}
